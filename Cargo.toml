[package]
name = "Game"
version = "0.1.0"
edition = "2024"

[profile.release]
opt-level = "z"     # 优化大小
lto = true           # 链接时优化
strip = true         # 去除调试符号
codegen-units = 1    # 单代码单元，利于优化

[build-dependencies]
embed-resource = "2"